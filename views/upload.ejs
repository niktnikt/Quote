<!DOCTYPE html>
<html>
<head>
	<!-- fonts -->
	<link href="https://fonts.googleapis.com/css?family=Dosis" rel="stylesheet">
	<link href="https://fonts.googleapis.com/css?family=Quicksand" rel="stylesheet">
	<link href="https://fonts.googleapis.com/css?family=Archivo" rel="stylesheet">
	<link href="https://fonts.googleapis.com/css?family=Montserrat" rel="stylesheet">
	
	<title>Upload</title>
	<link rel="stylesheet" type="text/css" href="/upload-style.css">
</head>
<body>
	<% if(user){ %>
		<% include partials/log-header.ejs %>
	<% }else{ %>
		<% include partials/header.ejs %>
	<% } %>

	<form id="upload-form" method="POST" action="/upload/">
		<label for="category">Category</label>
		<input type="text" name="category" autofocus>
		<label for="quote">Quote</label>
		<textarea maxlength="200" type="text" name="quote" placeholder="Please type your quote here..."></textarea>

		<% if(message != ''){ %>
			<p id="success-message"><%= message %></p>
		<% } %>
		<% if(errMessage != ''){ %>
			<p id="error-message"> <%= errMessage %> </p>
		<% } %>

		<input type="submit" value="Submit">
	</form>
</body>
</html>